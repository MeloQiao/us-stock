name: Sync to HF Spaces

on:
  # Sync disabled — re-enable when code is stable
  # push:
  #   branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to HF Spaces
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add hf https://meloqiao:$HF_TOKEN@huggingface.co/spaces/meloqiao/us-stock
          git push hf main --force
