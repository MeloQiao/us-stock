# Data
yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.24.0

# Backtest (optional — pandas fallback used if vectorbt unavailable)
# Heavy numba/llvmlite deps can timeout on HF Spaces; uncomment for local use:
# vectorbt>=0.26.2

# UI
streamlit>=1.32.0
plotly>=5.18.0

# Alpaca paper trading
alpaca-py>=0.20.0

# Scheduling
APScheduler>=3.10.4

# HF Dataset storage
huggingface_hub>=0.21.0
datasets>=2.18.0

# HTTP (Feishu alerts)
httpx>=0.27.0

# Optimisation (portfolio max-Sharpe + walk-forward weight optimizer)
scipy>=1.11.0

# ML Regime Classifier (Layer 2) — optional but strongly recommended
# xgboost and lightgbm are tried first; sklearn is always available as fallback
scikit-learn>=1.3.0
xgboost>=2.0.0

# Utils
python-dotenv>=1.0.0
pytz>=2024.1
exchange-calendars>=4.5.0

# A-share data (CN market)
akshare>=1.12.0
